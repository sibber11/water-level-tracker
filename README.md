# php based firebase admin sdk

## features
- receive distance data from an endpoint and store it temporarily in a file
- on receiving the data send it to firebase notifying the user

## requirements
- php 8.1+
- firebase admin sdk
- curl
- file system

## installation
1. clone the repository
2. run `composer install`
3. run `php index.php`